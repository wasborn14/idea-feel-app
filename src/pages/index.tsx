import type { NextPage } from "next";
import styled from "styled-components";

const Home: NextPage = () => {
  return (
    <Container>
      <h1>test</h1>
    </Container>
  );
};

export default Home;

const Container = styled.div`
  color: green;
`;
